"""
    Module that contains the heuristic class and heuristic functions, for
    ease of versioning and organization (specially the heuristic functions,
    if we need to test them individually).
    The imports are here so that 'heuristic' can still be accessed as a module.
"""

from .h_funct import *
from .heuristic import *


